# AGENTS.md

This file is generated by RuleDev Kit.
Follow these rules when modifying this repository.

## Project Context

RuleDev Kit is a Rules as Code toolkit for AI-driven development.

This repository is also developed with AI assistance.
Therefore, AI agents must follow the project rules strictly and avoid making broad, speculative, or inconsistent changes.

The goal of this project is to provide a CLI that can:

- define development rules in `.rules/rules/*.yml`
- compile rule definitions into AI-readable instruction files such as `AGENTS.md`
- check source files against rule definitions
- support repeatable, rule-based AI development workflows

## ai-workflow

### AI開発の基本手順

- Rule ID: `ai.workflow.basic`
- Severity: `warning`

実装前に、必ず既存のファイル構成・命名規則・実装方針を確認してください。

推測だけで新しい設計やディレクトリ構造を作らないでください。  
既存の `src/commands`、`src/core`、`.rules/rules` の責務に合わせて実装してください。

実装は次の順番で進めてください。

1. 既存実装を確認する
2. 変更方針を小さく決める
3. 必要最小限のファイルだけ変更する
4. `npm run check` を実行する
5. `npm run build` を実行する
6. 必要に応じて `ruledev compile` / `ruledev check` を実行する
7. 変更内容と確認結果を説明する

大きな変更を一度に行わないでください。  
CLI、ルール読み込み、Markdown生成、チェック処理は責務を分けて実装してください。

## repository-structure

### ディレクトリ責務

- Rule ID: `project.repository-structure`
- Severity: `error`

このプロジェクトでは、主な責務を以下のように分けます。

```txt
src/
├── cli.ts
├── commands/
│   ├── init.ts
│   ├── compile.ts
│   └── check.ts
└── core/
    ├── load-rules.ts
    └── render-agents.ts
```

各ファイルの責務は以下です。

- `src/cli.ts`
  - CLIコマンドの登録のみを行う
  - 複雑な処理を書かない

- `src/commands/*.ts`
  - CLIコマンド単位の処理を実装する
  - ユーザーへの出力や終了コードの制御を行う

- `src/core/*.ts`
  - ルール読み込み、変換、レンダリング、検証などの中核ロジックを実装する
  - CLIに依存しすぎない純粋な処理を優先する

新しい機能を追加するときは、CLI登録・コマンド処理・中核ロジックを混ぜないでください。

## typescript

### TypeScript実装ルール

- Rule ID: `typescript.strict-style`
- Severity: `error`

TypeScriptは strict 前提で実装してください。

以下を守ってください。

- `any` を安易に使わない
- 型定義を `src/core` 側に寄せる
- Node.js 標準モジュールは `node:` prefix を使う
- ESM前提で import/export を書く
- 相対importではビルド後の NodeNext に合わせて `.js` 拡張子を使う

例:

```ts
import fs from "node:fs/promises";
import path from "node:path";
import { loadRules } from "../core/load-rules.js";
```

## cli

### CLI設計ルール

- Rule ID: `cli.command-design`
- Severity: `error`

CLIコマンドは `ruledev` を前提にしてください。  
`rbd` は既存Linuxコマンドと衝突する可能性があるため、今後は使わないでください。

現在の基本コマンドは以下です。

```bash
ruledev init
ruledev compile
ruledev check
```

新しいコマンドを追加する場合は、以下を守ってください。

- コマンド名は短く、意味が明確であること
- `src/cli.ts` にはコマンド登録だけを書くこと
- 実処理は `src/commands/<command>.ts` に分離すること
- 複雑なロジックは `src/core` に切り出すこと

## rules

### ルール定義ファイルの扱い

- Rule ID: `rules.yaml-schema`
- Severity: `error`

ルールは `.rules/rules/*.yml` に定義します。

基本形式は以下です。

```yaml
id: example.rule-id
title: ルールタイトル
category: category-name
severity: error
instruction: |
  AIに読ませる開発ルールを記載します。
check:
  type: text
  include:
    - "src/**/*.ts"
  pattern:
    - "example"
```

ルールを追加・変更するときは、以下を守ってください。

- `id` は一意にする
- `title` は人間が読んで意味がわかる名前にする
- `category` は既存カテゴリと整合させる
- `severity` は `error`、`warning`、`info` のいずれかにする
- `instruction` はAIがそのまま守れる具体的な内容にする
- `check` は検証可能なルールだけに付ける

## generated-files

### 生成ファイルの扱い

- Rule ID: `project.generated-files`
- Severity: `warning`

`AGENTS.md` は RuleDev Kit によって生成されるファイルです。

ルール内容を変更したい場合は、原則として `AGENTS.md` を直接編集するのではなく、`.rules/rules/*.yml` を変更してから以下を実行してください。

```bash
ruledev compile
```

ただし、初期開発中にルール内容を検討する目的で `AGENTS.md` を一時的に編集することは許可します。  
その場合も、最終的には `.rules/rules/*.yml` に反映してください。

## validation

### 実装後の確認

- Rule ID: `project.validation`
- Severity: `error`

実装後は必ず以下を実行してください。

```bash
npm run check
npm run build
```

ルール生成やルール検証に関わる変更をした場合は、追加で以下も実行してください。

```bash
ruledev compile
ruledev check
```

エラーが出た場合は、エラー内容を読んで原因を説明し、最小の修正で直してください。  
エラーを無視したり、関係ない大きな変更で回避したりしないでください。

## scope-control

### 変更範囲の制御

- Rule ID: `ai.scope-control`
- Severity: `warning`

AIエージェントは、依頼された範囲を超えて不要な変更をしないでください。

特に以下は禁止します。

- 依頼されていない大規模リファクタリング
- 不要な依存パッケージ追加
- package manager の変更
- ESM / CommonJS 方針の変更
- CLI名の変更
- 既存コマンドの破壊的変更
- 生成ファイルだけを直して元ルールを直さない変更

必要な変更が大きくなる場合は、先に理由を説明してください。

## frontend

### Client Componentで直接fetchしない

- Rule ID: `nextjs.no-client-fetch`
- Severity: `error`

このルールは、Next.js プロジェクト向けのサンプルルールです。

データ取得は Server Component、`page.tsx`、`actions.ts` に集約してください。  
Client Component 内で直接 `fetch` を呼ばないでください。

RuleDev Kit 自体は現在 CLI ライブラリであり、Next.js アプリではありません。  
そのため、このルールは主にサンプル・プリセット用途として扱ってください。
